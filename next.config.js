/** @type {import('next').NextConfig} */
const nextConfig = {}

// next.config.js
module.exports = {
    images: {
      domains: ['file.rendit.io', 'i.imgur.com', 'images.unsplash.com'], // Adicione o hostname da fonte da imagem aqui, sem o protocolo "https://"
    },
  }
  
